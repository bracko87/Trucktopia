/**
 * .env.example
 *
 * Example env variables for running the migration function locally or in your hosting provider.
 * Do NOT commit real secrets into the repository. Use your hosting provider's secret store.
 *
 * Usage (Netlify):
 * - Set SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY and MIGRATE_ADMIN_TOKEN in Netlify UI -> Site settings -> Build & deploy -> Environment.
 *
 * Usage (locally, PowerShell example):
 * $env:SUPABASE_URL="https://your-project.supabase.co"
 * $env:SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
 * $env:MIGRATE_ADMIN_TOKEN="a-strong-random-token"
 *
 * Notes:
 * - SUPABASE_SERVICE_ROLE_KEY must be kept secret (it grants full DB access).
 * - MIGRATE_ADMIN_TOKEN is an application-level token that the client/UI uses to authorize the migration request.
 */

/* Supabase endpoint (example) */
SUPABASE_URL=https://your-project.supabase.co

/* Supabase service role key (store in hosting secrets) */
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SERVICE_ROLE_KEY

/* Admin token required by the migration endpoint (choose a long random string) */
MIGRATE_ADMIN_TOKEN=REPLACE_WITH_STRONG_ADMIN_TOKEN

/* Optional: table where migration records are stored */
MIGRATION_TABLE=migrated_collections